# Commission task from Serggin

## Running app

In the project's root folder execute command:

$ php script.php path-to-csv-file

for example:

$ php script.php input.csv

## Testing app

In the project's root folder execute command:

$ composer test
